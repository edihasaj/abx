// abx semantic version. Bumped automatically by the release workflow on each
// tagged release (see .github/workflows/release.yml). Keep the literal on one
// line so the workflow's sed can rewrite it.
export const VERSION = "0.1.1";
